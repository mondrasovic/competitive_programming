#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int a1, b1, t1, a2, b2, t;
    while ((cin >> a1 >> b1 >> t1 >> a2 >> b2 >> t) && (a1 > 0)) {

    }

    return 0;
}
